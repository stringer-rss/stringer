<div id="action-bar">
  <%= render_partial :feed_action_bar %>
</div>

<% unless @feeds.empty? %>
<div id="feeds">
  <ul id="feed-list">
    <% @feeds.each do |feed| %>
      <%= render_partial :feed, { feed: feed } %>
    <% end %>
  </ul>
</div>
<% else %>
  <div id="add-some-feeds">
    <p><%= t('feeds.index.add_some_feeds', :add => '<a href="/add_feed">'+t('feeds.index.add')+'</a>') %></p>
  </div>
<% end %>
